import styled from "@emotion/styled";

const style = (Component) => styled(Component)`
  .titulo-strong {
    font-size: 32px;
    font-weight: 700;
    line-height: 58px;
    letter-spacing: 0em;
    text-align: center;
  }
  .titulo {
    font-size: 32px;
    font-weight: 400;
    line-height: 58px;
    letter-spacing: 0em;
    text-align: center;
  }
`;

export default style;
