import React from 'react'

const index = (props) => {
    return (
        <section className={props.altura+" "+props.cor+" "+props.padding}>
            
        </section>
    )
}

export default index
