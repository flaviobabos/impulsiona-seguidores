import styled from '@emotion/styled';

const style = (Component) => styled(Component)`

h4{
    font-family: Lato;
font-size: 26px;
font-style: normal;
font-weight: 700;
letter-spacing: 0em;
text-align: left;

}
`;

export default style;