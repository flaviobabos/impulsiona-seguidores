import styled from '@emotion/styled';

const style = (Component) => styled(Component)`

.text{
font-size: 26px;
font-weight: 700;
letter-spacing: 0em;
text-align: left;
cursor:pointer;
}
`;

export default style;